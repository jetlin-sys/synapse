---
name: whalecloud-dev-tool-module-function
description: "模块功能技能 - 以需求澄清文档为唯一需求依据进行功能拆分，匹配功能架构模块列表，结合代码确认后输出需改造的模块清单和涉及的核心类列表。"
label: 模块功能技能
---

# 模块功能技能

将原始需求按功能拆分，匹配功能架构模块列表，**以需求澄清文档为分析起点与唯一需求依据**，再结合产品架构文档与代码确认后输出需改造的模块清单。**严禁臆断想象**，所有结论必须有实际存在的内容作为依据。

## 研发会议室：工单目录读代码 / 文档

`room_opened` 已将产品资产落盘到工单目录。**只读下列路径，禁止臆造目录。**

| 用途 | 路径约定 | 说明 |
|------|----------|------|
| 工单根目录 | `{WORK_ORDER_DIR}` | 系统提示「产品工作区路径」或委派参数，形如 `work/<scope_id>/` |
| 需求澄清文档 | **固定路径（勿传参）** | `{WORK_ORDER_DIR}/archive/需求分析/req_clarify/需求澄清.md` |
| 产品代码 | `{PRODUCT_CODE_ROOT}/<repo_name>/` | 默认 `{WORK_ORDER_DIR}/code/<repo_name>/` |
| 产品文档 | `{PRODUCT_DOC_ROOT}/<doc_type>/` | 默认 `{WORK_ORDER_DIR}/doc/<doc_type>/`；架构文档在 `产品架构/` |

- **需求澄清（最高优先级）**：本技能的一切需求理解、功能拆分、范围界定**必须**基于固定路径 `{WORK_ORDER_DIR}/archive/需求分析/req_clarify/需求澄清.md`；未读取该文档前不得开展后续分析。**仅此唯一路径**，禁止尝试其他路径或回退路径。
- **FUNCTIONAL_ARCH / TECH_ARCH**：优先读 `{PRODUCT_DOC_ROOT}/产品架构/`
- **代码确认**（Step 3）：在 `{PRODUCT_CODE_ROOT}` 各仓库目录内检索，路径标注为 `<repo_name>/相对路径`

---

## Parameters

| Parameter | 必填 | 说明 / 示例 |
|-----------|------|----------------|
| `PROD` | 是 | 产品名称 |
| `WORK_ORDER_DIR` | 是 | 工单工作目录（研发会议室注入），如 `work/21881451`。**必须**提供，用于解析固定需求澄清路径 `{WORK_ORDER_DIR}/archive/需求分析/req_clarify/需求澄清.md` |
| `PRODUCT_CODE_ROOT` | 否 | 产品代码根目录，默认 `{WORK_ORDER_DIR}/code` |
| `PRODUCT_DOC_ROOT` | 否 | 产品文档根目录，默认 `{WORK_ORDER_DIR}/doc` |

> **知识来源（优先级从高到低）**：
> 1. **需求澄清文档**（固定路径见上表）— 需求范围、功能要点、澄清结论、约束与依赖的**唯一权威来源**；功能拆分与模块匹配必须可回溯至该文档中的具体条目
> 2. **产品架构文档**（FUNCTIONAL_ARCH、TECH_ARCH）— 模块列表与架构划分的参考依据
> 3. **代码** — 验证模块存在性与核心类的最终事实依据
>
> 任何无法从上述来源验证的结论必须标注 `[待确认]`，严禁臆断想象。

---

## 核心约束（违反本技能视为未完成）

### A. 技能执行输出限制

**整个技能执行过程中，大模型的输出应遵循以下原则：**

1. **生成文档阶段**：按照下方「输出内容格式」章节的结构输出结果
2. **异常中止时**：可直接输出中止原因和缺失信息，无需走文档格式

### B. 需求澄清文档优先（强制约束）

- **必须先读需求澄清文档**：Step 1 成功读取 `{WORK_ORDER_DIR}/archive/需求分析/req_clarify/需求澄清.md` 之前，禁止进行功能拆分、模块匹配或代码确认。
- **分析必须基于需求澄清文档**：功能点拆分、IN/OUT 范围界定、模块匹配依据、新增模块判定，均须引用需求澄清文档中的具体章节或条目（范围定义、功能要点、澄清结论、约束与依赖）。
- **禁止绕过需求澄清文档**：不得直接依据原始需求描述、口头理解或 FUNCTIONAL_ARCH 推断需求范围；原始需求仅作背景展示，**不得**作为功能拆分与模块识别的依据。
- 需求澄清文档与 FUNCTIONAL_ARCH 描述冲突时，以**需求澄清文档**为准；冲突点须在输出中标注说明。

### C. 产品知识体系事实依赖（强制约束）

- 模块识别**必须严格依赖**以下三类事实来源，且按优先级使用，不得凭空臆断或想象：
  | 优先级 | 事实来源 | 说明 |
  |--------|---------|------|
  | **1（需求依据）** | **需求澄清文档** | `{WORK_ORDER_DIR}/archive/需求分析/req_clarify/需求澄清.md` — 范围定义、功能要点、澄清结论、约束与依赖 |
  | **2（架构参考）** | **产品架构文档** | `{PRODUCT_DOC_ROOT}/产品架构/`（FUNCTIONAL_ARCH、TECH_ARCH） |
  | **3（代码验证）** | **代码** | 优先 `{PRODUCT_CODE_ROOT}/<repo_name>/`；通过文件系统或检索工具读取，禁止臆造路径 |
- 模块识别中凡涉及**文件路径、接口、类名**等结论，必须在代码或文档中有对应支撑依据，并标注来源；功能点与模块的对应关系还须标注对应的需求澄清文档条目。
- 无法通过上述事实来源验证的条目必须标注 **`[待确认]`**，不得虚构。
- 若某文档源未获取成功，涉及该文档的分析标注 `[待补充-文档未获取]`。

### D. 输出范围约束

- 本技能**只输出需改造的模块清单**，不涉及具体改造方案、改造内容或实现细节
- 输出的模块名称必须与 `FUNCTIONAL_ARCH.md` 中的模块名称保持一致
- 超出功能架构模块列表的改造范围，标识为新增模块

### E. 事实驱动的模块确认（强制约束）

- `FUNCTIONAL_ARCH.md` 是**指导性文档，不一定完全正确**，必须通过实际代码确认其准确性
- 功能点**必须**从需求澄清文档提取后再按功能架构模块列表匹配，再通过代码确认匹配结果是否合理
- 代码确认可能发现功能架构描述与实际代码不一致的情况，此时以代码为准
- 每个修改模块都必须经过代码确认
- 每个新增模块需确认功能架构中确实没有可覆盖的模块
- 所有确认结论必须有具体的文件路径或文档引用作为依据

---

## 工作流程

```
Step 0 — 参数校验与环境准备
  0a. 校验必填参数：PROD、`WORK_ORDER_DIR`（缺任一则**中止**）
  0b. 解析固定路径（**唯一地址，不可变更、不可回退**）：
      `CLARIFY_DOC_PATH = {WORK_ORDER_DIR}/archive/需求分析/req_clarify/需求澄清.md`
      （勿由调用方传入 `CLARIFY_DOC` 或其他路径参数）
  0c. **解析工单目录**：`CODE_ROOT = PRODUCT_CODE_ROOT`（缺省 `{WORK_ORDER_DIR}/code`），`DOC_ROOT = PRODUCT_DOC_ROOT`（缺省 `{WORK_ORDER_DIR}/doc`）

Step 1 — 获取项目资料（需求澄清文档优先）
  1a. **仅读取** `CLARIFY_DOC_PATH`（即 `{WORK_ORDER_DIR}/archive/需求分析/req_clarify/需求澄清.md`）；若不存在则**立即中止**，注明该固定路径，**不得**尝试其他路径
  1b. 在 `{DOC_ROOT}/产品架构/` 读取 FUNCTIONAL_ARCH、TECH_ARCH 相关 `.md`；FUNCTIONAL_ARCH 缺失则**中止**

Step 2 — 基于需求澄清文档提取功能点并匹配架构模块
  2a. **基于需求澄清文档**（`CLARIFY_DOC_PATH`）提取以下结构化信息（本步骤的核心输入，不可跳过）：
      - **范围定义**：IN（涉及范围）和 OUT（排除范围）
      - **功能要点**：需求涉及的功能点列表
      - **澄清结论**：关键确认点的总结
      - **约束与依赖**：模块依赖、技术约束、数据依赖
  2b. 读取 FUNCTIONAL_ARCH.md，提取"核心功能详解"章节中的**功能模块列表**（每个模块的名称、功能描述、涉及的源代码文件），记为 ARCH_MODULE_LIST
  2c. **仅依据 2a 提取的功能要点**（不得引入需求澄清文档未列出的功能点），与 ARCH_MODULE_LIST 的划分粒度对齐，形成最终的 FEATURE_POINTS：
      - 若功能要点按页面粒度划分（如"前台页面-用户管理页面"），保持页面级别
      - 若功能要点按服务粒度划分（如"后台服务-用户管理"），保持服务级别
      - 若功能要点粒度与 ARCH_MODULE_LIST 不对齐，按 ARCH_MODULE_LIST 的粒度重新拆分
      - OUT 范围内的功能点排除，不纳入 FEATURE_POINTS
      - 每个 FEATURE_POINT 须标注其来源（需求澄清文档中的对应条目）

Step 3 — 功能点匹配与代码确认
  对 FEATURE_POINTS 中的每个功能点，依次执行以下匹配流程：

  3a. **匹配功能模块**：将该功能点与 ARCH_MODULE_LIST 中的模块逐一比对，找到最匹配的功能模块
      - 匹配依据（按优先级）：① 需求澄清文档中的范围定义、功能要点、约束与依赖；② 功能描述的语义相似度与业务领域归属
      - 一个功能点可能匹配到多个功能模块（如前台页面+后台服务都需改造）
      - 若无法匹配到任何已有模块 → 标记为"未匹配"，进入 **3d** 处理

  3b. **代码确认**（对每个匹配到的功能模块）：
      功能架构是指导性文档，必须通过实际代码确认匹配结论是否准确：
      - 在 `{PRODUCT_CODE_ROOT}`（或 `{WORK_ORDER_DIR}/code`）下各 `<repo_name>/` 目录中搜索模块关键类名/接口名
      - 在关键模块路径下搜索相关业务逻辑代码
      - 读取模块核心代码文件（路径格式：`<repo_name>/相对路径`）
      - **识别核心类**：从搜索结果中提取该模块的核心类（Controller/Service/DAO/Model 等），记录类名与**工单代码目录内**文件路径

  3c. **确认结论**（以代码为准）：
      - 代码确认该模块确实存在且需要改造 → 确定为**修改模块**，记录核心类列表和来源路径
      - 代码发现功能架构描述与实际代码不一致 → 以代码为准，调整匹配结论和核心类
      - 代码确认该模块已能覆盖需求 → 从修改列表中排除
      - 代码无法验证 → 标注 `[待确认]`

  3d. **新增模块判定**（对 3a 中"未匹配"的功能点）：
      - 若功能点无法匹配到 ARCH_MODULE_LIST 中的任何已有模块
      - 或匹配到的模块经代码确认后，发现功能范围差异过大、不适合在该模块内改造
      - 则标识为**新增模块**，说明新增原因（须引用需求澄清文档中对应功能要点），并通过代码检索识别核心类

Step 4 — 生成模块功能文档
  4a. 汇总：需改造的模块清单（修改模块+新增模块）
  4b. 按照「输出内容格式」章节的结构输出结果
```

---

## Error Handling

| 情况 | 处理 |
|------|------|
| 缺少必填参数（PROD 或 `WORK_ORDER_DIR`） | **中止**，列出缺失项 |
| 需求澄清文档 `{WORK_ORDER_DIR}/archive/需求分析/req_clarify/需求澄清.md` 不存在 | **立即中止**（本技能无法在无需求澄清文档的情况下执行），注明该固定路径 |
| 需求澄清文档存在但无法提取功能要点 | **中止**，说明文档缺失的关键章节 |
| `{DOC_ROOT}/产品架构/` 无 FUNCTIONAL_ARCH | **中止** |
| TECH_ARCH 缺失 | 继续，仅以 FUNCTIONAL_ARCH 为准 |
| 代码检索无结果 | 标注 `[待确认]`，不得虚构内容 |

---

## 输出内容格式

模块功能文档按以下结构直接输出 Markdown：

```markdown
# 模块功能

> **生成时间**: {当前时间}
> **需求名称**: {需求名称}
> **状态**: {状态}

---

## 需求澄清依据

> 以下分析基于 `{WORK_ORDER_DIR}/archive/需求分析/req_clarify/需求澄清.md`

- **范围定义（IN/OUT）**：{从需求澄清文档摘录}
- **关键澄清结论**：{从需求澄清文档摘录}

---

## 需求功能拆分

> 功能点均来自需求澄清文档，不得超出其 IN 范围或包含 OUT 范围内容。

| # | 功能点 | 说明 | 澄清文档来源 |
|---|--------|------|-------------|
{逐个列出功能点}
| {序号} | {功能点名称} | {功能点说明} | {需求澄清文档中的对应条目} |

---

## 修改模块

> 以下模块来自功能架构文档，经代码确认后确定需要改造。

| # | 功能模块名称 | 对应功能点 | 核心类 | 澄清文档依据 | 确认依据 |
|---|-------------|----------|--------|-------------|---------|
{逐个列出修改模块}
| {序号} | {模块名称} | {对应功能点} | {核心类列表} | {需求澄清文档条目} | {代码/架构确认依据} |

## 新增模块

> 以下功能点超出功能架构模块列表范围，需新增模块。

| # | 模块名称 | 对应功能点 | 核心类 | 新增原因 | 澄清文档依据 | 确认依据 |
|---|---------|----------|--------|---------|-------------|---------|
{逐个列出新增模块}
| {序号} | {模块名称} | {对应功能点} | {核心类列表} | {新增原因} | {需求澄清文档条目} | {代码/架构确认依据} |
```

---